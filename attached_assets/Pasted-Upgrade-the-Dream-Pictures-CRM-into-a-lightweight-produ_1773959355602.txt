Upgrade the Dream Pictures CRM into a lightweight, production-ready business CRM.

IMPORTANT GOAL

The system must remain:

* Fast on Fly.io small machines
* Minimal Neon PostgreSQL storage usage
* No heavy background jobs or cron tasks

---

PAYMENT SYSTEM (CRITICAL FIX)

Implement full payment update system.

Balance logic:

Balance = Total Price - SUM(all payments)

Features:

* Add "Record Payment" button in Pending Payments table
* Allow multiple payments per work
* Do NOT overwrite old payments

After payment:

* Auto update balance
* If balance = 0 → mark as PAID (green badge)
* Remove from pending list

Validation:

* Prevent overpayment
* Show error if payment > remaining balance

---

GLOBAL SEARCH (TOP NAVBAR)

Add search bar in header.

Search across:

* Client name
* Phone number
* Work description

Use simple LIKE query.

Limit results (max 20).

---

DATE FILTER SYSTEM

Add filter:

* Today
* This Week
* This Month
* Custom Range

Apply to:

* Payments
* Expenses
* Work

Use indexed fields (date columns).

---

AUTO BACKUP (LIGHTWEIGHT)

Add button:

Export All Data (CSV)

Export:

* clients.csv
* works.csv
* payments.csv
* expenses.csv

Do NOT use cron jobs.

Manual backup only.

---

QUICK ACTION BUTTONS

On dashboard:

* Add Client
* Add Work
* Record Payment
* Add Expense

---

SMART ALERTS (NO BACKGROUND JOBS)

On dashboard load calculate:

* Pending Work count
* Pending Payments total
* Upcoming Birthdays (30 days)
* Upcoming Anniversaries

Show alerts at top.

---

EXPENSE SYSTEM (OPTIMIZED)

Keep existing structure.

Add:

* Category totals using SUM
* Monthly expense total

Avoid storing duplicate data.

---

INCOME CALCULATION

Income = SUM(payments)

Do NOT store income separately.

---

TOP CLIENTS

Calculate dynamically:

SUM(total work value per client)

Limit to top 5.

---

PERFORMANCE RULES

* Use simple SELECT + SUM queries only

* Add indexes on:
  client_id
  work_id
  date
  status

* Avoid joins where possible

* Use LIMIT in queries

* No logs storage

---

UI RULES

* Keep dark clean UI
* Mobile friendly
* Highlight:

Pending Work → RED
Pending Payment → ORANGE
Paid → GREEN

---

DATA SAFETY

* Prevent duplicate clients (same phone)
* Prevent negative balance
* Validate all inputs

---

NO HEAVY FEATURES

DO NOT ADD:

* Cron jobs
* Background workers
* Large analytics
* Image/file storage
* Auto messaging services

---

FINAL GOAL

System should be:

* Fast
* Simple
* Daily business ready (Photography + Cyber Cafe)
* Zero unnecessary load
* Minimal database usage

---

OPTIONAL (ONLY IF LIGHTWEIGHT)

Allow:

* WhatsApp click-to-send links
* CSV export only (no Excel libraries)

---

END RESULT

A lightweight professional CRM that runs smoothly on Fly.io and does not exhaust Neon free storage.
